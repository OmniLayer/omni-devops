# Force all text files to have LF endings 
# Since the Vagrant-controlled VMs will be reading text files from the synced directories
# These files must have UNIX-compatible LF endings
* text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.jar binary
*.7z binary


